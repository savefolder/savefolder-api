version: "3"
services:
    mongo:
        image: mongo:latest
    redis:
        image: redis:latest
    web:
        build:
            dockerfile: docker/dev.docker
            context: ..
        links:
            - mongo
            - redis
        ports:
            - "8080:8080"
        volumes:
            - "..:/app"
